<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Optional sensors: recording works without these, the corresponding streams are just absent. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <!-- Allows registering sensor listeners above 200 Hz on API 31+ (normal permission). -->
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <!-- Lets EpochRift Pocket read the film roll through RollProvider. Signature-level:
         granted only to an app signed with this app's key, never on request. -->
    <permission
        android:name="se.lth.math.videoimucapture.permission.READ_ROLL"
        android:protectionLevel="signature" />

    <!-- Lets EpochRift Pocket delete one session at a time through RollProvider, once the
         footage is safe elsewhere. Separate from READ_ROLL and signature-level too: the
         provider is exported, so without a write permission anything could delete. -->
    <permission
        android:name="se.lth.math.videoimucapture.permission.DELETE_ROLL"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApp">
        <activity
            android:name=".CameraCaptureActivity"
            android:exported="true"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RollActivity"
            android:exported="false"
            android:label="@string/roll_toolbar"
            android:parentActivityName=".CameraCaptureActivity" />
        <activity
            android:name=".SessionActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            android:parentActivityName=".RollActivity" />
        <provider
            android:name=".RollProvider"
            android:authorities="se.lth.math.videoimucapture.roll"
            android:exported="true"
            android:readPermission="se.lth.math.videoimucapture.permission.READ_ROLL"
            android:writePermission="se.lth.math.videoimucapture.permission.DELETE_ROLL" />
    </application>

</manifest>
